{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.16.0"
  },
  "dependencies": {
    "hardhat-circom": "^3.3.2",
    "snarkjs": "^0.7.0"
  },
  "scripts": {
    "test": "hardhat test",
    "circom": "hardhat circom --deterministic --debug --verbose",
    "hardhat": "hardhat node",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts"
  }
}
